import { Box, Heading } from "@chakra-ui/react";

const Cart = () => {
    return(
        <Box maxWidth={"1320px"} margin={"auto"} color={"teal"}>
            <Heading>/Cart</Heading>
        </Box>
    );
}

export default Cart;